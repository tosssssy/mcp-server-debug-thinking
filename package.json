{
  "name": "mcp-server-debug-iteration",
  "version": "0.1.0",
  "description": "MCP server for systematic code debugging and iteration tracking",
  "type": "module",
  "bin": {
    "mcp-server-debug-iteration": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prepare": "npm run build"
  },
  "keywords": [
    "mcp",
    "debugging",
    "code-iteration"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=16"
  }
}